<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pi5_ecomove">

    <!-- Permissões necessárias -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PI5_Ecomove"
        android:networkSecurityConfig="@xml/network_security_config"> <!-- Configuração de segurança para ClearText -->

        <!-- Atividades -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" android:exported="true" />
        <activity android:name=".RegisterActivity" android:exported="true" />
        <activity android:name=".HomeActivity" android:exported="true" />
        <activity android:name=".OfferActivity" android:exported="true" />
        <activity android:name=".RequestActivity" android:exported="true" />
        <activity android:name=".TripsActivity" android:exported="true" />
    </application>

</manifest>
